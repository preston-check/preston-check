# Python dependencies for the auto-evolving threat-intel pipeline.
# Pinned to compatible-version ranges; the CI workflows install these via
# `pip install -r tools/requirements.txt`.

bashlex>=0.18,<1.0
pyyaml>=6.0,<7.0
cryptography>=42.0,<50.0
requests>=2.32,<3.0
