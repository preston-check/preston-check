# Session log 2026-08-12 — alert-flood fix, Node 24 migration, AI add-on

Handoff record for the next session. Three work streams landed, all
verified and pushed to master; open items are at the end.

## 1. Failed-run alert flood — resolved

The 4-emails/day watchdog alert flood (since Aug 3) traced to a single
root cause: promotion PR #640 (opened 2026-08-02 by orchestrate run
30754673773) never merged because GitHub returned a 502 during the
self-merge step, then wedged in "Merge already in progress" through all
three retries. The PR sat open as the designed audit trail and the
watchdog escalated it every 6 hours. The stale-PR numbering-collision
prediction in `tools/watchdog.py` step 4 held exactly: the PR's check
number 1157 had been reassigned on master (`1157-cve-2026-50390-strict`)
and its CVE re-landed as `1160-cve-2026-50384-middle`. Git said
MERGEABLE; merging would have duplicated check id 1157.

Resolution per the documented backlog protocol: closed #640 with an
explanatory comment, deleted the branch, no re-merge — unpromoted CVEs
re-propose automatically. A dispatched verification watchdog run came
back "All quiet". Nothing needed a code change; every component behaved
as designed. A transient GitHub API failure during self-merge is now a
known way a single promotion PR goes stale (recorded in session memory).

## 2. Workflow runtime migration to Node 24 — commit `5ba3ef80`

GitHub is deprecating the Node 20 action runtime; every run logged
forced-Node-24 warnings. All 23 workflow files moved to each action's
lowest major that declares node24, chosen after reading release notes:
checkout v5, setup-python v6, setup-node v5 (auto-caching addition is a
no-op here — no packageManager fields anywhere), upload-artifact v6 (v5
still defaulted to node20), the Pages trio configure-pages v6 /
upload-pages-artifact v5 / deploy-pages v5 (v3 and v4 of the composite
pin a node20 upload-artifact internally), docker qemu/buildx/login v4,
build-push v7, gh-release v3, wrangler-action v4 (aligns the deploy CLI
with the `npx wrangler@4` already used in auth-deploy and d1-backup;
the action installed Wrangler 4.86.0 and deployed cleanly).
setup-homebrew stays SHA-pinned per the 2026-07-13 security review.
README/docs CI examples updated to checkout@v5.

Verified live on push: workflow-lint, Tests, landing-page deploy, and
all three Worker deploys (auth/billing/telemetry) green with zero Node
deprecation annotations. STILL UNEXERCISED: `release.yml` — no release
has been cut since the bump (last release run 2026-08-10). Its syntax
passed actionlint and inputs were verified compatible, but the first
post-bump release is the real test; the watchdog's tag-without-release
check covers a failure. Watch for it.

## 3. AI add-on — PR #749 merged (`af9a8ec2` + `b505415a`)

Preston-Check is now loadable into Claude, Llama (Ollama), Kimi and any
OpenAI-compatible stack. Full design: `docs/ai-addon.md`; user docs:
`ai-addon/README.md` and `ai-addon/MANUAL.md` (+ generated PDF in the
package). Architecture in one line: generated auditor personas carry
the judgment (compiled from every core/verified/accepted PRESTON_META
block — never the proposed tier), and a stdlib-only stdio MCP server
(`ai-addon/mcp/server.py`: scan_path / list_checks / explain_check)
carries the deterministic evidence. Fine-tuning was explicitly rejected
(daily-evolving catalog, hallucination risk, no attestations).

Components: `tools/build_personas.py` (single writer of
`ai-addon/personas/`; deterministic; 11 personas — 3 postures mapped
from the strict/middle/permissive variant system + 8 compliance lenses;
ingestion source tags and CWE labels are excluded from the frameworks
axis); `tools/build_ai_addon_package.py` (self-contained distributable
into `dist/`, scanner stamped from newest v* tag, personas regenerated
at build, manual PDF via pandoc+weasyprint with table-pagination CSS);
`ai-addon/install-ai-addon.sh` (non-destructive; --claude / --ollama
act). Built artifact this session:
`dist/preston-check-ai-addon-1.8.300.tar.gz` (0.8 MB, local only —
dist/ is gitignored).

Verification: server --selftest green (full JSON-RPC surface + planted
secret fixture — FAIL via P-01's Client/src branch, WARN via its Go
branch); live stdio MCP session verified; package re-verified from a
clean extraction; PDF persona table confirmed to repeat headers across
its page break.

## Open items for next session

1. Watch the first `release.yml` run since the action bumps (docker
   qemu/buildx/login v4, build-push v7, gh-release v3). Watchdog
   escalates if it breaks.
2. Owner decision: attach the AI add-on package to GitHub releases —
   requires touching release.yml (the workflow with the 17-day-outage
   history), deliberately not done unasked.
3. Owner decision: wire `tools/build_personas.py` into the orchestrate
   promotion leg so personas regenerate as check batches land.
4. Observation from fixture testing: P-01's primary branch greps only
   the hardwired roots `Common/src`, `Registration/src`, `Client/src`,
   `Payments-logic/src`; root-level secrets in non-Go/Rust files escape
   it. Generalization candidate.
5. Pre-existing uncommitted local work (NOT from these sessions, left
   untouched, repeatedly stashed/restored around rebases): modified
   `.githooks/README.md` and `.preston-check/state/partner_feed.json`;
   untracked `.githooks/post-commit`, `.githooks/post-merge`,
   `docs/shared-sources.md`, `tools/export_sources.py`. This looks like
   the in-progress wallet-verify shared-sources work.

## State at session end

master = `b505415a` (+ whatever ingest committed since); all CI green
including the security self-audit; watchdog quiet; catalog at 284 core
+ 611 accepted checks, newest tag v1.8.300 and climbing.
